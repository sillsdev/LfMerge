mode: Mainline
next-version: 1.0
branches:
  live:
    tag:
    regex: (origin/)?live
    is-mainline: true
  qa:
    mode: ContinuousDeployment
    tag: beta
    regex: (origin/)?qa
  master:
    mode: ContinuousDeployment
    tag: alpha
    regex: (origin/)?master
ignore:
  sha: []
