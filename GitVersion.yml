mode: Mainline
next-version: 1.0
branches:
  live:
    tag:
    regex: (origin/)?live
    is-mainline: true
  qa:
    tag: beta
    regex: (origin/)?qa
  master:
    tag: alpha
    regex: (origin/)?master
ignore:
  sha: []
